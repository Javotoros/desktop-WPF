<Window x:Class="DesktopApp.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login" Height="250" Width="400"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Vertical" Grid.Row="0">
            <TextBlock Text="Email:" Margin="0,0,0,5"/>
            <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" Height="25"/>
        </StackPanel>

        <StackPanel Orientation="Vertical" Grid.Row="1" Margin="0,10,0,0">
            <TextBlock Text="Contraseña:" Margin="0,0,0,5"/>
            <PasswordBox x:Name="txtPassword" Height="25"/>
        </StackPanel>

        <Button Content="Login" Grid.Row="2" Margin="0,20,0,0" Height="30" Width="100"
                HorizontalAlignment="Center"
                Command="{Binding LoginCommand}" 
                CommandParameter="{Binding ElementName=txtPassword}"/>
    </Grid>
</Window>
