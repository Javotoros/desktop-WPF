<UserControl x:Class="DesktopApp.Views.Reservation.ListReservationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="600">
    <Grid>
        <StackPanel Orientation="Vertical" Margin="10">
            <Grid Margin="0,0,0,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <!-- Izquierda (vacío o título) -->
                <StackPanel Orientation="Horizontal">
                    <Button Content="Nueva Reserva" Width="140" Height="35" Margin="0,0,0,10" Click="Nueva_Reserva"/>
                    <Button Content="Cancelar Reserva" Width="140" Height="35" Margin="10,0,0,10" Command="{Binding CancelarReservaCommand}"/>
                    <Button Content="Eliminar" Width="150" Height="35" Margin="10,0,0,10" Click="Eliminar_Reserva"/>
                </StackPanel>

                <!-- Derecha (filtro) -->
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <CheckBox Content="Ocultar canceladas" Margin="20,0,0,10" IsChecked="{Binding OcultarCanceladas, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

                    <TextBox x:Name="txtBuscar" Width="200" Height="35" Margin="10,0,0,10" Text="{Binding TextoBusqueda, UpdateSourceTrigger=PropertyChanged}" FontSize="15"/>

                    <Button Content="Limpiar" Command="{Binding LimpiarCommand}" Height="35" Margin="20,0,0,10" Width="75" />
                </StackPanel>
            </Grid>

            <DataGrid x:Name="dgReservation"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  Margin="10" 
                  ItemsSource="{Binding Reservas}"
                  SelectedItem="{Binding ReservaSeleccionada, Mode=TwoWay}">

                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="*" />
                    <DataGridTextColumn Header="Usuario(DNI)" Binding="{Binding UserDNI}" Width="*" />
                    <DataGridTextColumn Header="Usuario(Nombre)" Binding="{Binding UserNombre}" Width="*" />
                    <DataGridTextColumn Header="Habitaciones" Binding="{Binding RoomNumbers}" Width="*" />
                    <DataGridTextColumn Header="Entrada" Binding="{Binding CheckIn, StringFormat=dd/MM/yyyy}" Width="*" />
                    <DataGridTextColumn Header="Salida" Binding="{Binding CheckOut, StringFormat=dd/MM/yyyy}" Width="*" />
                    <DataGridTextColumn Header="Estado" Binding="{Binding Status}" Width="*" />
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>
    </Grid>
</UserControl>
