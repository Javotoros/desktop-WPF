<UserControl x:Class="DesktopApp.Controls.RoomsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:controls="clr-namespace:DesktopApp.Controls"
             mc:Ignorable="d" 
             Height="Auto" Width="Auto">
    <Grid>
        <Grid.ColumnDefinitions >
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="240"/>
            <ColumnDefinition Width="*"/>

        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Column="1" Grid.Row="1" Background="{StaticResource BackgroundBrush2}" Padding="12">

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Habitaciones"
                       FontSize="18"
                       FontWeight="SemiBold"
                       Margin="4,0,0,12"
                       Foreground="{StaticResource PrimaryBrush}"/>

                <ListBox Grid.Row="1"
                     Style="{StaticResource LeftTabListStyle}"
                     ItemContainerStyle="{StaticResource LeftTabItemStyle}"
                     SelectedIndex="1">
                    <ListBoxItem>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Crear habitación" Foreground="{StaticResource PrimaryBrush}" VerticalAlignment="Center"/>
                        </StackPanel>
                    </ListBoxItem>
                    <ListBoxItem>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Listado habitaciones" Foreground="{StaticResource PrimaryBrush}" VerticalAlignment="Center"/>
                        </StackPanel>
                    </ListBoxItem>
                </ListBox>


            </Grid>
        </Border>
        <controls:RoomsListView Grid.Column="2" Grid.Row="1"/>
    </Grid>
</UserControl>
